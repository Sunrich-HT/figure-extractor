# The PDF path — caption detection, layout, cropping, contact sheet — needs
# only PyMuPDF.
PyMuPDF>=1.24.0

# Only needed for HTML sources (`extract <html url>` / `--prefer html`).
beautifulsoup4>=4.12.0

# Only for the no-rasterizer fallback (bootstrap/no_rasterizer_extractor.py),
# which is for runtimes that have pypdf but not PyMuPDF.
pypdf>=4.0.0
